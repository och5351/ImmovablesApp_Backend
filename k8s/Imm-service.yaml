apiVersion: v1
kind: Service
metadata:
  name: immovables-serverside-svc
  namespace: ns-immovables
spec:
  ports:
    - name: "http"
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: immovables-serverside
  type: NodePort